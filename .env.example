# App runtime (Project Settings → API)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Migrations: Project Settings → Database → Connection string → URI
# Use Session mode or Direct (port 5432), not Transaction pooler.
DATABASE_URL=
