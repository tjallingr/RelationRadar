# Copy to .env and fill in from your Supabase project settings (API section).
# Leave both blank to run against the in-memory adapters instead, which needs
# no setup but loses all data when the dev server restarts.

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
